using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GlobalUtils : SingletonNoMono<GlobalUtils>
{
    // Start is called before the first frame update
    public int score = 0;
}
